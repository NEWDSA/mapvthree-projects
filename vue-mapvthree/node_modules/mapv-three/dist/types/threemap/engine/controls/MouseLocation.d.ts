/**
 * mouseLocation
 *
 * ```javascript
 * const mouseLocation = new MouseLocation(contorlContainer, enable, engine);
 * ```
 */
export class MouseLocation extends BaseControl {
    /** @private */
    private mouseMove;
}
import { BaseControl } from "./BaseControl";
